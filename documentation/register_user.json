{
    "endpoint": "/api/register_user",
    "method": "POST",
    "request": {
        "body": {
            "name": "string",
            "email": "string with email format",
            "password": "string"
        }
    },
    "response": {
        "success": "true"
    },
    "note": "saat user di create, bakalan sekaligus nge create Pembeli buat user itu"
}
